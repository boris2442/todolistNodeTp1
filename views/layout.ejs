<div class="container">
    <div class="box">
        <h3 class="">Ajouter une tache</h3>
        <form action="/submit" method="POST">
            <input type="text" placeholder="ajouter un texte" name="title">
            <input type="submit" value="Ajouter">
        </form>
    </div>


    <table>
        <thead>
            <tr>
                <th>#</th>
                <th>Todo</th>
                <th>Edité</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>

            <% todos.forEach(todo=> { %>
                <tr>
                    <td>
                        <%= todo._id %>
                    <td>
                        <%= todo.title %>
                    </td>
                    <td>
                        <button class="btn">
                           Editer <i class="fa-solid fa-pencil"></i>
                        </button>
                    </td>
                    <td>
                        <button class="btn">
                             Supprimer <i class="fa-solid fa-trash" style="color:brown"></i>
                        </button>
                    </td>
                </tr>
                <% }) %>
                   
        </tbody>
    </table>
</div>